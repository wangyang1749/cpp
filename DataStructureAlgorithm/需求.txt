示列表中的所有单元格任务2：编写一个模块（一组函数）来处理简单的链表。 
列表中存储的值将是指向边缘类型的指针
模块必须提供的功能包括：
Ø  从值创建单元格（这需要内存分配）
Ø  在列表开头插入单元格
Ø  在列表末尾插入单元格
Ø  从列表开头弹出单元格
Ø  从列表末尾弹出单元格
Ø  搜索列表中是否存在某个单元格
Ø  销毁列表（这需要内存释放）–使用迭代算法
Ø  销毁列表（这需要内存释放）–使用递归算法
Ø  

-----------------
任务1：为图表准备结构 
实际上，节点不仅仅是数字，边可以存储多个值。例如，节点可以有一个名称，边缘可以与容量相关联，但也可以存储一些成本值。
 
Ø  创建两种类型（C struct）来表示节点和边，具有以下功能：
显示节点/显示边
初始化一个节点/初始化一个边-从作为参数给定的一些值
初始化节点/初始化边缘-从用户键盘输入的某些值
